"""Fill the 网安学院本科实习手册 template with the AI Meeting ASR project."""

from __future__ import annotations

import re
from pathlib import Path

import docx
from docx.oxml.ns import qn

PROJECT_ROOT = Path(__file__).resolve().parent.parent
DOCS = PROJECT_ROOT / "docs"
SRC_DOCX = DOCS / "网安学院本科实习手册_AI会议语音转写与检索_已填写.docx"
MD_OUT = DOCS / "实习手册_9月1日-9月18日_文字稿.md"

DAYS = [
    ("2026年9月1日",
     "今天正式开始本科生实习。上午集中阅读了《智能体开发实战》课程下发的实验任务书，重点理解本次实习要完成的第15题“AI会议语音转写与检索”。通过梳理任务要求，我明确了该题目需要实现四类基础能力：会议音频转写、带时间戳的文字输出、关键词检索和时间定位，此外还要完成可选功能自动会议摘要。课程特别强调所有项目必须按照统一的 Skill 加 Script/CLI 接口开发，以便被 nanobot 等 Agent Runtime 加载调用。我初步理解了 Agent 负责自然语言意图理解、Skill 负责约定接口、Script/CLI 负责确定性计算的层次关系，并确定以开源项目 FunASR 作为语音识别的核心。此外还注意到课程对低 Token 优化、安全白名单、日志脱敏和自动化测试都有明确要求。通过这一天的阅读和思考，我对整个实习任务有了整体认识，也制定了以“先跑通转写、再逐步扩展检索定位摘要、最后接入 Agent 和 GUI”为顺序的开发计划。"),
    ("2026年9月2日",
     "今天主要进行开发环境的准备和项目目录的搭建。我使用 Python 3.12 创建了独立的虚拟环境，并编写了 requirements.txt 来管理依赖，其中核心是 FunASR 及其依赖的 PyTorch、torchaudio、ModelScope 等。安装过程中发现 funasr 1.4.16 的 PyPI 包并不会自动安装 torch，需要显式安装 torch 和 torchaudio，我把这一点记录了下来。随后按照课程要求设计了项目的目录结构，划分出 src、cli、app、tests、docs、data、skills 等目录，分别存放核心实现、命令行入口、图形界面、测试、文档、数据与 Skill 定义。我还初始化了本地 Git 仓库并编写了 .gitignore，把虚拟环境、模型缓存、真实音频、转写产物、.env 等敏感或大文件排除在版本控制之外，避免把密钥和个人数据提交到仓库。通过今天的搭建，项目具备了规范、可复现的工程基础。"),
    ("2026年9月3日",
     "今天学习了 FunASR 的基本原理并开始设计语音识别模块。FunASR 是达摩院开源的一站式语音识别工具包，我选用 paraformer-zh 作为中文语音识别模型，它采用非自回归的 Paraformer 结构，兼顾了识别精度和推理速度；同时搭配 fsmn-vad 做语音端点检测，用来区分有效语音和静音，以及 ct-punc 做标点恢复，让识别结果更符合自然书写习惯。我理解了 ASR 的基本流程是音频输入、特征提取、声学建模、语言解码，再到标点后处理。在代码层面，通过 funasr 的 AutoModel 统一加载三个模型，并规划了 transcribe.py 模块，让它负责音频输入校验、模型调用和结构化输出。这一天让我对语音识别从单纯的调用过渡到了对内部流程有基本认识。"),
    ("2026年9月4日",
     "今天完成了会议语音转写的 Script/CLI 实现。我编写了 meeting_tool.py 作为统一命令行入口，并实现了 transcribe 子命令，支持传入音频路径，调用 FunASR 完成转写，输出 JSON 格式的结果。为了让转写结果更易读，还同时输出了 TXT 格式。在实现中特别注意了命令行程序的规范性：成功时返回退出码 0，失败时返回非 0 并输出包含 error 字段的 JSON，方便上层 Agent 判断。针对输出文件已存在的情况，设计了 --overwrite 参数和明确的覆盖确认逻辑。我还处理了 wav、mp3、m4a 等常见格式的校验，并在缺少 ffmpeg 时给出清晰的提示。通过对比也认识到，Agent 负责理解用户意图，而转写这类确定性工作必须由 Script 独立可靠地完成。"),
    ("2026年9月5日",
     "今天实现了带时间戳的会议文本结构。转写结果不再只是一整段文字，而是被组织成多个 segment，每个 segment 包含 start_ms 开始时间、end_ms 结束时间和 text 文本三个字段。为了得到句级时间戳，我了解到 FunASR 默认只返回字符级时间戳，需要在调用时传入 sentence_timestamp 参数才能生成句级信息。我据此修正了转写代码，让结果按句子切分并带上毫秒级起止时间。同时编写了时间格式化函数，把毫秒转换为 HH:MM:SS 的可读形式。带时间戳的结构让会议内容可以被定位和检索，是整个项目后续关键词搜索和时间定位的基础，也让转写结果具备可追溯性。"),
    ("2026年9月6日",
     "今天开发了关键词检索模块，实现 search 子命令。检索逻辑由 Python 在本地完成：遍历转写结果的各个 segment，对关键词做大小写不敏感的子串匹配，命中后返回关键词、开始时间、结束时间、对应句子以及前后若干句的上下文。我分别验证了中文关键词和英文关键词的检索效果，并对不存在的关键词做了友好处理，返回空结果而不是报错。这里体现了一个重要设计思想：先用 Python 完成搜索、排序、上下文截取和去重，只把必要的命中片段交给上层 Agent，而不是把整份长会议文本全部发送给模型。这种本地先过滤的思路既降低了 Token 消耗，也让检索结果更精准，同时命中结果按时间顺序排列，便于快速定位到会议中的相应位置。"),
    ("2026年9月7日",
     "今天实现了时间位置查询，即 locate 子命令。用户可以用 MM:SS、HH:MM:SS 或纯秒数等格式指定时间点，程序先把这些格式统一解析成毫秒，再在转写结果中查找包含该时间点的 segment，找不到精确包含时则取时间上最近的 segment，并返回它前后若干句作为上下文。我还处理了时间超出会议范围的情况，通过 out_of_range 字段明确标记，并补充了时间段查询，可以一次性返回某一段连续时间内的所有内容。在调试过程中，我针对 00:30 这类时间点反复验证，确保返回的内容确实来自对应时间附近。时间定位和关键词检索相互配合，让用户既可以从内容出发找到时间，也可以从时间出发还原当时说了什么。"),
    ("2026年9月8日",
     "今天设计并实现了会议自动摘要模块。摘要结果包含 topic 主题、summary 概要、key_points 主要讨论点、decisions 决策事项和 action_items 待办事项五个字段。为了在没有外部大模型的情况下也能运行，我实现了一个基于抽取式的本地 fallback：用 jieba 做中文分词，统计关键词并给句子打分，再选取高分句子作为要点；决策和待办则通过“决定、同意、待办、负责”等关键词在原文句子中抽取。我特别强调了一个原则：如果会议原文没有明确证据，就不凭空生成姓名、结论和行动项，而是输出“未识别到明确决策事项”，并用 notes 字段明确标注哪些内容未能识别。这种可解释、不编造的摘要方式更符合课程对可靠性的要求。"),
    ("2026年9月9日",
     "今天开发了自然语言 Agent Router，实现一个轻量的意图识别入口。router 用规则把用户的自然语言指令映射为四类意图：transcribe 转写、search 检索、locate 定位和 summarize 摘要。它通过关键词匹配判断意图，并解析出搜索关键词或时间参数。在调试中我发现了一个真实问题：因为“识别”被列入了转写关键词，“搜索语音识别”会被错误地路由成转写。我调整了意图判断的优先级，让检索、定位、摘要等更明确的意图先于转写判断，并为此补充了回归测试。这个经历让我理解到，自然语言接口看似简单，但关键词的歧义和优先级处理需要仔细设计，而底层的确定性工作仍应交给 Script 完成。"),
    ("2026年9月10日",
     "今天完成了 meeting-asr Skill 的开发。按照统一规范编写了 SKILL.md，其中包含 name、description、使用场景、何时调用与不调用、输入参数、输出格式、Script/CLI 调用命令、失败处理和安全边界，并给出了两个完整例子。Skill 强调调用真实的 meeting_tool.py 命令行，而不是只提供提示词模板。这样整个调用层次就很清晰：用户自然语言经过 Agent 和 Router 识别意图，再由 Skill 约定接口，最终落到 Script/CLI 驱动 FunASR 等能力模块。我还准备了 skills/meeting-asr 目录作为可被 nanobot 加载的 workspace skill。这一天让我体会到，Skill 的本质是把能力固化成可发现、可调用、可约束的接口。"),
    ("2026年9月11日",
     "今天实现了安全机制和日志模块。安全方面落实最小权限原则：只允许读写项目目录内 data/input、data/output 和 logs 等白名单目录，拒绝 ../../ 这类目录穿越，也拒绝 C:\\Windows 等越界的绝对路径；输出文件已存在时要求显式 --overwrite。日志采用 JSONL 格式，记录时间戳、动作、输入文件 basename、状态、耗时和错误类型，但绝不记录音频内容和敏感凭据。我还编写了 secret 脱敏函数，对 API Key、Token、密码等格式的字符串进行替换，防止它们进入日志。这一天的实践让我认识到，Agent 项目会处理文件和外部输入，安全边界和审计日志必须从一开始就内置，而不是事后补充。"),
    ("2026年9月12日",
     "今天完成了低 Token 和性能优化。核心思路是“大会议文本检索结果压缩”：先由 Python 在本地完成搜索、排序、上下文截取和去重，只把命中的少量片段交给模型，而不是把整份长会议文本全部发送。我实现了一个对比统计，分别估算完整 transcript 和仅命中片段的 Token 数，输出 compression_ratio、estimated_token_before、estimated_token_after 和 reduction_percent。在多段示例上，关键词“深度学习”的估算 Token 从 360 降到 125，减少约 65%。这些数字都来自实际示例的计算，不是预设的。这个优化直接减少了模型推理的输入量，也体现了“能本地确定处理就不调用大模型”的原则。"),
    ("2026年9月13日",
     "今天编写并完善了 pytest 自动化测试。测试覆盖了关键词检索的正常、多次命中、不存在关键词、中文和英文关键词，时间解析的 MM:SS 和 HH:MM:SS 格式，超出会议范围的处理，路径穿越拒绝，摘要结构，Agent 意图识别和空 transcript 等场景，最终全部 30 个用例通过，而且运行耗时极短，便于修改代码后随时回归。在整理测试的过程中，我特别保留了之前发现的路由问题的回归测试：“搜索语音识别”必须被识别为 search 而不是 transcribe。通过把 bug 固化成测试用例，我体会到自动化测试不仅能验证功能，更能防止已经修复的问题再次出现，是保证项目质量的重要手段。"),
    ("2026年9月14日",
     "今天开发了 Streamlit 图形界面，方便课程演示和老师现场验收。界面包含音频上传与选择、开始转写、转写结果、关键词输入与搜索、时间位置查询、生成会议摘要以及状态和错误提示等区域。我把之前实现的 CLI 能力直接复用到界面逻辑中，上传音频后调用 transcribe，搜索结果带时间戳显示，定位和摘要也都通过真实脚本完成，并提供了原始 JSON 的展开查看，便于核对底层数据。界面保持简洁，目标是让老师在一分钟左右的演示中就能看懂整个流程。我在本机启动了 Streamlit 服务，访问 localhost:8501，确认健康检查返回 200。GUI 让原本面向命令行的能力变得更直观，也方便后续录制演示视频。"),
    ("2026年9月15日",
     "今天进行了真实公开语音测试，用实际音频验证 FunASR 推理链路。我从 FunASR 官方和 PaddleSpeech 官方下载了公开的中文示例音频，以及一段英文示例，统一保存到 data/input 并做完整性校验，确认都能正常解码。随后用项目 CLI 对它们逐一转写，中文音频识别出了正确的句子，英文音频也完成了对照。基于真实转写结果，我又实际执行了关键词搜索、时间定位和摘要，确认各模块在真实输入下都能正常工作。这次测试让我把之前用示例 JSON 验证的逻辑替换成了真实 ASR 输出，最终标记 REAL_ASR_PIPELINE_TEST 为 PASS。需要说明的是，这些是公开朗读音频，不是真实私人会议。"),
    ("2026年9月16日",
     "今天构建了一段适合演示的合成模拟会议音频。为了模拟多人会议，我使用 edge-tts 的两个不同中文声音分别扮演主持人和成员，编写了一段关于智能体开发课程项目的会议对话，内容包含预算、数据集、下周三、测试、GitHub 等关键词，合成了约 67.5 秒的 16kHz 单声道音频，并保存了对应的人工原文作为 ground truth。随后用真实 FunASR 对它转写，得到 27 个带时间戳的 segment，时间戳单调递增。我还把识别文本与原文做了演示级的字符错误率计算，CER 约为 4.91%。这里要强调，这段音频是 TTS 合成的模拟会议，不是真实会议数据，CER 也只用于演示，不是模型性能 benchmark。"),
    ("2026年9月17日",
     "今天进行了 nanobot Agent Runtime 的集成验证。我在独立的虚拟环境中安装了 nanobot 0.3.5，配置了 DeepSeek 的 provider 和 deepseek-flash 模型，把 workspace 指向本项目目录，使 nanobot 能够发现 skills/meeting-asr 下的 Skill。随后用自然语言逐一触发转写、关键词搜索、时间定位和会议摘要四个任务，通过运行日志确认 nanobot 确实读取了 SKILL.md 并真实执行了 meeting_tool.py，而不是凭提示词编造答案。四项任务全部通过，最终标记 NANOBOT_RUNTIME_TEST 和 NANOBOT_INTEGRATION 为 PASS。这个环节把 Agent 到 Skill 到 Script/CLI 的完整链路真正跑通，也让我对 Agent Runtime 的工作方式有了直观认识。"),
    ("2026年9月18日",
     "今天进行综合验收和项目整理。我在 Streamlit 界面人工完成了完整流程：上传 synthetic_meeting_demo.wav、开始转写、查看带时间戳结果、搜索“预算”、定位 00:30、生成会议摘要，确认各字段显示正常，GUI_FINAL_CHECK 标记为 PASS。随后我用 Playwright 自动操作浏览器录制了约 56 秒的演示视频，并通过 ffmpeg 转换为 H.264 的 MP4，抽帧校验确认画面正常。最后整理了 README、实验报告、测试报告、开源项目报告、截图清单和验收视频脚本，并检查了 Git 提交记录。关于 GitHub 发布，我完成了本地仓库整理和发布前的敏感信息扫描，但组织仓库权限尚未就绪，因此目前状态是等待组织权限后再提交，没有把未完成的事项写成已完成。"),
]

REPORT = """一、实习背景与目的
本次实习围绕《智能体开发实战》课程展开，我选择的题目是第15题“AI会议语音转写与检索”。实习的目的是把课堂上学到的智能体开发方法落到一个真实可运行、可演示、可交付的项目上，完整经历从需求分析、环境搭建、核心算法集成到接口封装、测试验证和工程整理的整个过程。通过这个项目，我希望深入理解 Agent、Skill 与 Script/CLI 三层之间的关系，弄清楚大模型在智能体系统里到底应该负责什么、不应该负责什么，同时熟悉 FunASR 这一开源语音识别工具链，并在实践中培养安全意识和工程规范意识。这些能力的获得，仅靠阅读教材是远远不够的，必须通过亲自搭建系统、亲自调试、亲自验证才能真正掌握。同时，会议语音转写与检索这一题目具有明确的工程价值：会议内容往往信息量大、时间长、回看成本高，如果能够自动转写并支持按关键词和时间快速定位，就能显著提升会后整理和复盘的效率，这也让我从一开始就对项目抱有兴趣和动力。

二、实习任务与需求分析
任务要求把会议音频转写为带时间戳的文字，支持关键词检索和时间定位，并能返回相关会议内容与时间位置，可选功能是自动会议摘要。课程还规定所有项目必须统一按照 Skill 加 Script/CLI 的接口开发，以便被 nanobot 等 Agent Runtime 加载调用。经过需求分析，我把整个系统拆分为转写、检索、定位、摘要四个确定性能力，并在此基础上增加自然语言 Router 和 Skill 两层接口，同时明确安全白名单、日志脱敏、低 Token 优化和自动化测试等横切要求。这四个基础任务加一个可选功能，表面上互不相关，实际上都围绕同一份带时间戳的转写结果展开，因此数据结构的统一设计就变得特别关键。这一分解也让我意识到，需求分析不是简单罗列功能，而是要把模糊的自然语言需求转化为边界清晰、职责明确的模块，这样才能在后续实现中少走弯路。

三、技术方案与总体架构
系统采用分层架构：最上层是用户自然语言，经过 Agent/Router 识别意图，再由 meeting-asr Skill 约定接口，最终落到 meeting_tool.py 这个统一 CLI。CLI 之下是四个能力模块：FunASR 转写、关键词检索、时间定位和自动摘要，全链路都有安全层和日志层贯穿。核心能力用 Python 实现，检索和定位这类确定性工作完全在本地完成，只有确有必要时才把少量文本交给外部大模型。这样设计的最大好处是：即使没有大模型、没有网络，转写、检索、定位和抽取式摘要依然可以独立运行，系统的可靠性不依赖单一外部服务，同时也能显著节省 Token 成本。整个调用层次“自然语言到 Agent、Agent 到 Skill、Skill 到 Script、Script 到模型”非常清晰，每一层都有明确的输入输出约定，便于独立测试、独立替换和独立复用。

四、FunASR 语音识别模块
语音识别采用开源项目 FunASR，使用版本 1.4.16。我选用 paraformer-zh 作为中文识别模型，它采用非自回归的 Paraformer 结构，兼顾识别精度与推理速度；同时配合 fsmn-vad 做语音端点检测，用来区分有效语音和静音，ct-punc 做标点恢复，让识别结果更接近自然书写习惯。通过 funasr 的 AutoModel 把三个模型统一加载，即可对 wav、mp3、m4a 等常见音频完成识别。模型在首次转写时会从 ModelScope 下载并缓存，我还记录了 FunASR 版本、模型名称、Python 版本和 Torch 版本等信息，便于追溯和复现。FunASR 在整个系统中处于数据入口的位置，后续的检索、定位和摘要都建立在它的转写结果之上，因此转写的质量和时间戳的准确性直接决定了上层功能的效果。这一模块也是本项目与单纯的大模型聊天应用最根本的区别所在。此外，FunASR 本身是开源项目，采用 MIT 许可证，允许在课程项目中自由使用，这也符合课程“参考开源项目”的要求；我没有重新训练模型，而是直接复用预训练权重，因为通用中文会议转写场景下预训练模型已经足够好，重新训练既不经济也无必要。

五、会议转写和时间戳设计
转写结果被组织成多个 segment，每个 segment 包含 start_ms、end_ms 和 text 三个字段。为了得到句级时间戳，我起初直接调用模型，结果只得到字符级时间戳，整段音频被当成一句，经过查阅文档才明白需要在调用时传入 sentence_timestamp 参数，让 FunASR 返回按句切分的时间信息。修正后，每个 segment 用毫秒记录起止时间，再格式化为 HH:MM:SS 的可读形式。这种结构让会议内容可定位、可检索、可追溯，是整个项目的数据基础。转写同时输出结构化的 JSON 和易读的 TXT 两种格式，既方便程序处理，也方便人工阅读。此外我还做了时间戳单调性校验，确保相邻 segment 的起止时间按顺序排列，避免因切分或排序问题产生错乱。在时间戳的具体表示上，我统一使用毫秒作为内部单位，这样既能避免浮点精度问题，又便于和 FunASR 返回的毫秒级时间直接对齐，对外展示时再转换成可读形式，这种“内部统一、对外友好”的处理方式贯穿了项目始终。

六、关键词检索和时间定位
关键词检索由 Python 在本地完成，对 segment 做大小写不敏感的子串匹配，命中后返回关键词、时间戳、句子以及前后若干句的上下文，并支持限制返回结果数量，对不存在的关键词则友好地返回空结果而不是报错。时间定位支持 MM:SS、HH:MM:SS 和纯秒数等多种输入格式，程序先把它们统一解析成毫秒，再查找对应时间点的 segment，时间超出会议范围时用 out_of_range 字段明确标记。这两项能力互为补充，让用户既可以从内容出发找到时间，也可以从时间出发还原当时的会议内容。它们都不依赖大模型，完全由确定性算法完成，这也是本项目刻意强调的设计——只有把这类基础能力做扎实，整个系统才不会因为外部模型的不稳定而失效。

七、自动会议摘要
摘要模块输出 topic、summary、key_points、decisions 和 action_items 五个字段。在没有外部大模型时，采用基于 jieba 分词的抽取式本地 fallback：先做中文分词并统计关键词，再给句子打分选取要点，决策和待办则通过“决定、同意、待办、负责”等关键词在原文句子中抽取。这里我坚持一个原则：如果会议原文没有明确证据，就不凭空生成姓名、结论和行动项，而是如实输出“未识别到明确决策事项”。这样做的代价是摘要可能不够华丽，但换来的是可解释、可追溯、不编造，这比一份看似完整却夹带虚构内容的摘要更有价值。同时模块也预留了接入外部大模型的路径，只要配置好相应参数，就可以在本地抽取和生成式摘要之间切换，兼顾了可用性和扩展性。

八、Agent Router 与 Skill
自然语言 Router 用规则把用户指令映射为 transcribe、search、locate、summarize 四类意图，并解析出关键词和时间参数。meeting-asr Skill 通过 SKILL.md 定义 name、description、使用场景、输入输出、CLI 命令、失败处理和安全边界，并特别强调要调用真实的 meeting_tool.py 命令行，而不是只给出提示词模板。我还在项目里同时准备了根目录和 skills 目录两份 SKILL.md，其中 skills/meeting-asr 作为 nanobot 的 workspace skill，便于 Runtime 直接发现。这样 Agent 负责意图理解，Skill 负责接口约定，Script 负责确定性计算，三个层次各司其职、边界清晰。在开发 Router 时，我体会最深的一点是关键词歧义：例如“识别”既是转写关键词又出现在“语音识别”里，必须通过调整意图优先级来正确处理。

九、nanobot Runtime 集成
我在独立的虚拟环境中安装 nanobot 0.3.5，配置 DeepSeek 的 provider 和 deepseek-flash 模型，把 workspace 指向项目目录，使 nanobot 能够发现 skills/meeting-asr 下的 Skill。随后用自然语言逐一触发转写、搜索、定位、摘要四个任务，通过运行日志确认 nanobot 确实读取了 SKILL.md 并真实执行了 meeting_tool.py，而不是凭提示词编造答案，四项任务全部通过，最终 NANOBOT_RUNTIME_TEST 和 NANOBOT_INTEGRATION 均为 PASS。这次集成最有价值的地方在于，它证明了“Skill 被发现”和“Skill 被真正执行”是两件不同的事，只有拿到运行时日志里的工具调用证据，才能确认后者。在集成过程中还出现了一个插曲：nanobot 的 shell 默认使用系统 Python，而 FunASR 依赖装在项目虚拟环境里，为了完成转写，Agent 自动在系统 Python 里补装了依赖，这件事说明 Skill 文档里写清运行环境和使用哪个 Python 对 Runtime 能否正确执行至关重要。

十、安全机制
安全方面落实最小权限原则：只允许读写项目目录内的 data/input、data/output、logs 等白名单目录，拒绝 ../../ 这类目录穿越，也拒绝 C:\\Windows 等越界的绝对路径；输出文件已存在时要求显式 --overwrite，避免误覆盖。日志采用 JSONL 格式，只记录时间戳、动作、文件 basename、状态、耗时和错误类型，不记录音频内容和敏感凭据，并专门编写脱敏函数替换 API Key、Token、密码等格式的字符串。此外项目刻意不提供删除文件的能力，从根上降低误操作风险。我在测试里专门覆盖了路径穿越、越界绝对路径和敏感信息脱敏这几个用例，确保安全逻辑不是写在文档里的一句空话，而是真的能被自动化验证。更重要的是，任何 API Key、GitHub Token、密码和 nanobot 的 config.json 都不能进入 Git 仓库，因为这些是真实凭据，一旦进入版本历史就难以彻底清除。这些措施看似琐碎，但对于一个会处理文件和外部输入的 Agent 系统来说必不可少。

十一、低 Token 与性能优化
优化的核心是“大会议文本检索结果压缩”。先用 Python 在本地完成搜索、排序、上下文截取和去重，只把命中的少量片段交给模型，而不是把整份长会议文本全部发送。系统会输出 compression_ratio、estimated_token_before、estimated_token_after 和 reduction_percent 四个统计值，方便量化压缩效果。在多段示例上，关键词“深度学习”的估算 Token 从 360 降到 125，减少约 65%，这些数字都来自实际示例的计算，并非预设。之所以不把会议全文无条件放进模型上下文，是因为会议文本很长，既消耗 Token 又带来无关信息；Python 先检索、切分、筛选，只把必要片段交给 Agent，既省钱又聚焦。这个优化的本质，是尽量把可以确定性完成的工作留在本地，把宝贵且昂贵的模型调用留给真正需要语义理解的环节。除了检索结果压缩，项目在摘要、日志等环节也都避免把大段原始文本无谓地送入模型，从整体上控制了 Token 消耗。

十二、Streamlit GUI
为便于课程演示和老师现场验收，我开发了 Streamlit 图形界面，包含音频上传与选择、开始转写、转写结果、关键词输入与搜索、时间位置查询、生成会议摘要以及状态和错误提示等区域，所有界面逻辑都直接复用底层已经验证过的 CLI 能力，没有另起炉灶。界面在本机 localhost:8501 运行，健康检查返回 200，人工验收确认上传、转写、检索、定位和摘要都正常，GUI_FINAL_CHECK 为 PASS。一个简洁清晰的界面让原本面向命令行的能力变得直观，也让整个项目的价值在短时间内就能被看懂。

十三、测试与问题解决
项目使用 pytest 编写了 30 个自动化用例，覆盖关键词检索的正常、多次命中、不存在关键词、中英文关键词，时间解析的 MM:SS 与 HH:MM:SS，超出会议范围，路径穿越拒绝，摘要结构，Agent 意图识别和空 transcript 等场景，全部通过。开发过程中曾发现一个真实的问题：“搜索语音识别”会被错误地路由为转写，原因是“识别”被列入了转写关键词。我调整了意图判断的优先级，并把这个案例固化成回归测试。这件事让我明白，自动化测试不仅能验证功能，更能防止已经修复的问题再次悄悄出现。除了这 30 个单元测试，我还在不同阶段做了真实运行层面的验证，包括公开音频的真实推理、合成会议的完整流程和 nanobot 的 Runtime 调用，这些端到端的验收性测试与单元测试相互补充，共同保证了项目的可靠性。

十四、真实音频与模拟会议验证
我用 FunASR 官方和 PaddleSpeech 官方的公开中文音频以及一段英文示例做了真实推理，确认转写、检索、定位、摘要各模块在真实输入下都正常，REAL_ASR_PIPELINE_TEST 为 PASS。随后为了做完整的会议演示，我用 edge-tts 的两个不同中文声音合成了一段约 67.5 秒的模拟会议音频，真实转写出 27 个带时间戳的 segment，时间戳单调递增，并计算了约 4.91% 的演示级字符错误率。这里必须如实说明：合成会议是 TTS 生成的模拟素材，不是真实会议数据，CER 也只用于演示，不代表模型 benchmark，我没有把两者混为一谈。之所以要引入合成会议素材，是因为公开示例音频普遍较短，不足以展示关键词检索、时间定位和摘要之间的连贯流程，而合成素材可以在不涉及隐私的前提下完整走通演示。

十五、Demo 视频和最终工程整理
我用 Playwright 自动操作本地 Streamlit 录制了约 56 秒的演示视频，完整记录了上传、转写、检索、定位和摘要的全过程，再用 ffmpeg 转换成 H.264 的 MP4，并通过抽取 10% 到 90% 的五帧画面校验确认没有黑屏且内容正常。工程上我整理了 README、实验报告、测试报告、开源项目报告、截图清单和验收视频脚本，检查了 Git 提交记录和 .gitignore。关于 GitHub 发布，我完成了本地仓库整理和发布前的敏感信息扫描，但组织仓库权限尚未就绪，因此目前是等待权限后再提交，没有把未完成的事项写成已完成。

十六、实习中遇到的问题
实习过程中主要遇到了几类问题：一是 funasr 的 PyPI 包并不会自动安装 torch，导致最初运行时报出 PyTorch 未找到的告警；二是 FunASR 默认只返回字符级时间戳，整段音频被当成单句，句级时间信息缺失；三是自然语言 Router 存在关键词歧义，出现了错误路由；四是直接运行脚本时发生相对导入失败；五是本机缺少 ffmpeg，也没有现成的真实会议音频；六是在验证阶段需要区分公开音频、合成会议和真实会议三者的性质，不能把它们混为一谈。此外，FunASR 首次运行要从 ModelScope 下载模型，首次下载依赖网络；mp3、m4a 等格式又依赖 ffmpeg，本机最初没有安装，必须明确提示而不是静默失败。在 nanobot 上，我一开始只做到了 Skill 能被 discovery，后来又通过运行日志确认它真正执行了 meeting_tool.py 才算通过。关于 GitHub，我也理解了组织仓库和个人仓库的权限区别。这些问题分别涉及依赖环境、模型接口、逻辑设计、运行方式和测试素材几个不同层面，解决它们的过程本身就是一次很有价值的工程训练。

十七、解决方法
针对上述问题，我分别采取了对应的措施：显式安装 torch 和 torchaudio，并在 requirements 中写清楚；调用 FunASR 时传入 sentence_timestamp 参数获取句级时间戳；调整 Router 的意图优先级并补充回归测试；为脚本增加“脚本/包”双模式导入兼容；对缺少 ffmpeg 的情况给出明确的安装提示，并用公开音频和合成会议素材完成了真实验证。在排查路由问题时，我先复现了“搜索语音识别”被识别成 transcribe 的现象，再定位到“识别”这个关键词，最后通过调整意图判断顺序并新增回归测试保证修复不再复发。在 nanobot 上，我把验证从“文件能被发现”推进到“运行时真的调用脚本”，用运行日志里的工具调用记录作为证据。这些方法和思路我都记录在项目文档里，便于复现。整个过程中我始终坚持一个原则：遇到问题先定位真实原因，再针对性修复，而不是靠猜测或掩盖。

十八、实习收获
通过这次实习，我完整地经历了一个智能体应用从需求分析、环境搭建、核心实现、测试验证到界面演示和工程整理的全程，掌握了 FunASR 的集成方法、Agent 与 Skill 与 Script 的分层设计、安全白名单与日志脱敏、低 Token 优化和自动化测试。更重要的是，我养成了用真实运行结果验证代码的习惯：不虚构转写结果，不编造测试数据，不把未完成的工作写成已完成。这种严谨的态度，我认为比学会任何一个具体框架都更重要，也是这次实习留给我最深刻的东西。同时，这次实习也锻炼了我查阅文档、定位问题和动手解决问题的能力，从依赖缺失、接口参数到路由歧义，每一个问题的解决都让我对相关技术有了更扎实的理解，也让我不再满足于“能跑就行”，而是更关注代码的规范、可测试和可维护。此外，我对 Python 工程项目结构、Git 版本控制和 pytest 回归测试有了更深的理解，也明白 Web GUI 只是后端能力的入口；对开源项目，我学会了关注许可证、依赖和可复现性。总体而言，我从原来只会调用模型，进步到能独立做出一个可运行、可测试、可交付的智能体系统。

十九、对智能体开发的认识
通过这次实习，我对智能体开发形成了更清晰的认识：它的关键不是堆砌大模型调用，而是清晰的职责分层和可靠的确定性底座。Agent 擅长理解自然语言和规划任务，但搜索、时间解析、摘要抽取这些确定性工作应该由 Script 在本地完成，模型只在真正需要语义理解时才介入。Skill 则把能力固化成可发现、可约束、可复用的接口。安全、日志和测试虽然不直接产生业务功能，但它们是让系统可信赖的基础设施。智能体的“智能”不仅来自大模型，也来自这套精心设计的工程结构。具体到本项目，Agent 负责解析自然语言意图，但不应亲自去做搜索和时间计算这类确定性工作；Skill 是意图和工具之间的接口描述；Script/CLI 提供稳定、可测试、可复现的底层能力；FunASR 负责真实语音识别。相比让大模型直接处理所有文本，这种分层更可靠，也更利于调试、安全控制和后续扩展。面向未来，我认为智能体开发会越来越强调工具的可组合性和可验证性，如何把模型、确定性工具、安全约束和清晰接口组织成一个可靠整体，才是工程上的核心挑战。

二十、总结与后续学习计划
本项目目前已经完成了核心功能、真实验证、GUI、自动化测试和 Demo 视频，整体达到课程验收要求，仅剩 GitHub 组织发布因为权限问题而暂时阻塞。后续可以继续探索的方向还很多：多人会议场景下的说话人分离与角色标注；长会议的分块与流式转写；更强的语义检索；会议决策和待办事项的结构化抽取；以及更完善的权限控制。在技术栈上，我还计划继续学习 Agent Runtime、Tool Calling、RAG 等内容。这次实习让我对智能体开发建立了系统认识，也让我看到自己还有不少可以提升的空间，我会把这份项目作为后续学习的起点，持续完善。"""


def _set_run_font(run, size: float = 12, font: str = "宋体") -> None:
    run.font.size = docx.shared.Pt(size)
    run.font.name = font
    rpr = run._element.get_or_add_rPr()
    rfonts = rpr.get_or_add_rFonts()
    rfonts.set(qn("w:eastAsia"), font)


def _set_cell_text(cell, text: str, size: float = 12) -> None:
    chunks = [c for c in text.split("\n") if c.strip()]
    # remove all paragraphs after the first
    for p in list(cell.paragraphs[1:]):
        p._element.getparent().remove(p._element)
    first_p = cell.paragraphs[0]
    for r in list(first_p.runs):
        r._element.getparent().remove(r._element)
    for i, chunk in enumerate(chunks):
        p = first_p if i == 0 else cell.add_paragraph()
        run = p.add_run(chunk)
        _set_run_font(run, size)


def _fill_daily(tables, idx: int, date: str, content: str) -> None:
    t = tables[idx]
    # date cell (row 0, right cell)
    _set_cell_text(t.rows[0].cells[2], date, size=12)
    # content cell (row 3, merged big cell)
    _set_cell_text(t.rows[3].cells[0], content, size=12)


def _write_markdown() -> None:
    lines = ["# 实习手册文字稿（2026年9月1日—2026年9月18日）", ""]
    lines.append("## 每日实习记录")
    lines.append("")
    for date, content in DAYS:
        lines.append(f"### {date}")
        lines.append("")
        lines.append(content)
        lines.append("")
    lines.append("## 实习总结报告")
    lines.append("")
    lines.append(REPORT)
    MD_OUT.write_text("\n".join(lines) + "\n", encoding="utf-8")


def _count_cjk(text: str) -> int:
    return len(re.findall(r"[\u4e00-\u9fff]", text))


def main() -> int:
    doc = docx.Document(str(SRC_DOCX))
    tables = doc.tables
    # fill internship date range in personal-info table (row 4 = 日期)
    info_rows = tables[1].rows
    for r in info_rows:
        label = r.cells[0].text.strip()
        if label.startswith("日期"):
            _set_cell_text(r.cells[1], "2026年9月1日—2026年9月18日", size=12)
            break
    for i, (date, content) in enumerate(DAYS):
        _fill_daily(tables, 2 + i, date, content)
    # report table is index 33 (rows: header, label, content)
    report_cell = tables[33].rows[2].cells[0]
    _set_cell_text(report_cell, REPORT, size=12)
    doc.save(str(SRC_DOCX))
    _write_markdown()

    report_cjk = _count_cjk(REPORT)
    daily_lens = [_count_cjk(c) for _, c in DAYS]
    print("filled days:", len(DAYS))
    print("daily CJK char range:", min(daily_lens), "-", max(daily_lens))
    print("report CJK chars:", report_cjk)
    print("saved:", SRC_DOCX)
    print("markdown:", MD_OUT)
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
