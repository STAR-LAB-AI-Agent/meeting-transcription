# 实习手册截图清单

> 每张图都是真实界面/输出，请在实际运行后截图，不要伪造。

## 图1 项目目录

- 截什么：文件资源管理器或 `tree` 命令显示项目结构。
- 关键信息：`src/`、`cli/`、`app/`、`tests/`、`docs/`、`SKILL.md` 等目录齐全。
- 手册描述：项目整体目录结构。

## 图2 Python 环境和依赖安装

- 截什么：`python --version` 与 `pip install -r requirements.txt` 成功输出。
- 关键信息：Python 3.10~3.12 版本号、`Successfully installed funasr ...`。
- 手册描述：开发环境与依赖安装过程。

## 图3 FunASR 模型成功加载

- 截什么：运行转写时 FunASR 打印的模型下载/加载日志。
- 关键信息：`paraformer-zh`、`fsmn-vad`、`ct-punc` 加载成功。
- 手册描述：FunASR 模型集成与加载。

## 图4 CLI 音频转写

- 截什么：`python src/meeting_tool.py transcribe --audio data/input/xxx.wav`。
- 关键信息：命令与 `segments` JSON 输出。
- 手册描述：脚本/CLI 转写能力。

## 图5 带时间戳结果

- 截什么：`data/output/xxx.json` 或 Markdown 转写结果。
- 关键信息：每句 `start_ms` / `end_ms` / `text`。
- 手册描述：带时间戳转写结果。

## 图6 关键词检索

- 截什么：`search --keyword "预算"` 输出。
- 关键信息：`hit_count`、命中句、时间戳、上下文。
- 手册描述：关键词检索与上下文返回。

## 图7 时间定位

- 截什么：`locate --time "01:30"` 输出。
- 关键信息：`target_time`、对应片段、`out_of_range` 为 false。
- 手册描述：时间点/时间段定位。

## 图8 自动会议摘要

- 截什么：`summarize` 输出。
- 关键信息：`topic`、`summary`、`key_points`、`decisions`、`action_items`。
- 手册描述：自动会议摘要。

## 图9 Streamlit GUI

- 截什么：浏览器中的 Streamlit 界面。
- 关键信息：上传/转写/检索/定位/摘要按钮与结果。
- 手册描述：图形化演示界面。

## 图10 pytest 测试通过

- 截什么：`pytest -q` 输出。
- 关键信息：`30 passed`。
- 手册描述：自动化测试结果。

## 图11 Skill 文件

- 截什么：`SKILL.md` 文本。
- 关键信息：`name`、`description`、CLI 命令、安全边界、示例。
- 手册描述：统一 Skill 接口。

## 图12 nanobot 加载 Skill（如实际完成）

- 截什么：nanobot 加载 `meeting-asr` 后的调用结果。
- 关键信息：Agent Runtime 成功调用 CLI。
- 手册描述：Agent Runtime 接入（若未安装 nanobot，此图不截，说明即可）。

## 图13 Token 优化结果

- 截什么：`search --keyword "深度学习" --stats` 输出。
- 关键信息：`compression_ratio`、`estimated_token_before/after`、
  `reduction_percent`。
- 手册描述：大会议文本检索结果压缩。

## 图14 Git log

- 截什么：`git log --oneline`。
- 关键信息：多个逻辑清晰的 commit。
- 手册描述：版本管理。

## 图15 GitHub 仓库页面

- 截什么：浏览器中的 GitHub 仓库首页。
- 关键信息：README、目录、提交历史（确认无敏感文件）。
- 手册描述：代码开源与托管。
