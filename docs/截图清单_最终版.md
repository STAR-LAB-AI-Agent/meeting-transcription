# 截图清单（最终版）

> 每张图均为真实界面/输出，按下方命令实际运行后截图，不要伪造。
> 截图放入 `docs/screenshots/`，建议命名 `01_project_tree.png` 等。

## 【必须截图】

### 01 项目目录
- 截什么：`tree /F` 或资源管理器。
- 显示什么：`src/ cli/ app/ tests/ docs/ skills/ scripts/ examples/ data/`、
  `SKILL.md`、`PROJECT_STATUS.md`。
- 实验报告章节：4 系统总体架构。

### 02 Python/FunASR 环境
- 截什么：`pip list`（过滤 funasr/torch/modelscope/streamlit）。
- 显示什么：Python 3.12.10、funasr 1.4.16、torch 2.14.0、modelscope 1.40.1。
- 章节：5 技术路线。

### 03 FunASR 模型实际加载
- 截什么：转写时的模型加载日志。
- 显示什么：`Loading pretrained params`、`funasr version: 1.4.16`、rtf。
- 章节：6 FunASR 集成。

### 04 官方公开音频真实 ASR
- 截什么：`transcribe --audio data/input/sample_01_clear_chinese.wav` 输出。
- 显示什么：“欢迎大家来体验达摩院推出的语音识别模型。”。
- 章节：15 实际测试结果。

### 05 60~90 秒模拟会议音频
- 截什么：`soundfile` 读取 `synthetic_meeting_demo.wav` 的信息（16kHz/mono/67.5s）。
- 显示什么：时长 >= 45s、16kHz mono PCM16。
- 章节：15 实际测试结果。

### 06 带时间戳转写结果
- 截什么：`data/output/synthetic_meeting_demo.txt`。
- 显示什么：多段 `HH:MM:SS` 起止时间 + 文本。
- 章节：15 实际测试结果。

### 07 “预算”关键词检索
- 截什么：`search --transcript ... --keyword "预算"`。
- 显示什么：`hit_count`、时间戳、上下文。
- 章节：9 关键词检索。

### 08 00:30 时间定位
- 截什么：`locate --transcript ... --time "00:30"`。
- 显示什么：`target_time`、`out_of_range:false`、对应片段。
- 章节：10 时间定位。

### 09 自动会议摘要
- 截什么：`summarize --transcript ...`。
- 显示什么：topic/summary/key_points/decisions/action_items/notes。
- 章节：11 会议摘要。

### 10 Streamlit GUI
- 截什么：`scripts\run_web.bat` 后浏览器 http://localhost:8501。
- 显示什么：上传/转写/时间戳/搜索/定位/摘要/状态提示。
- 章节：5/15。此图需人工点击。

### 11 pytest 全通过
- 截什么：`python -m pytest -q`。
- 显示什么：`30 passed`。
- 章节：14/15 测试。

### 12 skills/meeting-asr/SKILL.md
- 截什么：`skills/meeting-asr/SKILL.md` 文本。
- 显示什么：`name: meeting-asr`、`description`、CLI 命令、安全边界。
- 章节：7 Skill/Script 设计。

### 13 nanobot Installed Skill
- 截什么：`docs/real_validation/nanobot_skill_discovery.txt` 或 nanobot WebUI。
- 显示什么：`meeting-asr` 被 `SkillsLoader` 发现（source=workspace）。
- 章节：7。

### 14 nanobot 自然语言调用 Skill
- 截什么：`nanobot agent -m "搜索预算" ...` 的对话输出。
- 显示什么：Agent 识别 meeting-asr Skill 并调用 CLI。
- 章节：7。待模型配置后截。

### 15 nanobot 调用 meeting_tool.py 的证据
- 截什么：`nanobot agent --logs` 中的工具调用/执行日志。
- 显示什么：`meeting_tool.py` 被执行、退出码 0。
- 章节：7。待模型配置后截。

### 16 Token 优化
- 截什么：`search --keyword "深度学习" --stats`（examples/demo_transcript.json）。
- 显示什么：`compression_ratio 0.3472`、`360 -> 125`、`65.28%`。
- 章节：13 低 Token 优化。

### 17 git log
- 截什么：`git log --oneline`。
- 显示什么：10+ 个逻辑 commit。
- 章节：17 解决方法（版本管理）。

### 18 GitHub Repository
- 截什么：浏览器 GitHub 仓库首页。
- 显示什么：README、目录、提交历史（无敏感文件）。
- 章节：17。待登录发布后截。

## 【后续截图】（完成后补）

- 14/15 nanobot Runtime：待模型配置。
- 18 GitHub：待 gh 登录并发布。
